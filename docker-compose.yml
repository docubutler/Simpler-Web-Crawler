version: '3.8'
services:
  web-crawler:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Uncomment if you want to mount code for live reload during development
    # volumes:
    #   - .:/app
    # command: ["conda", "run", "-n", "web-crawler", "uvicorn", "web-crawler:app", "--host", "0.0.0.0", "--port", "8000"]
